<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Incubate Ind Task</title>

    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">

</head>

<body>

    <div class="w3-container w3-margin w3-padding-64">
        <div class="w3-center w3-margin">
            <form action="/display" method="GET">



                <div class="row ">
                    <button class="w3-btn w3-blue w3-xlarge w3-margin-bottom" type="submit" name="action">Display Details :)</button>
                </div>

            </form>

        </div>
        {{#if array}}
        <table class="w3-table-all w3-card w3-animate-opacity">
            <thead>
                <tr class="w3-blue">
                    {{#each array.[0]}}
                    <th>{{@key}}</th>
                    {{/each}}
                </tr>
            </thead>
            <tbody>
                {{#each array}}
                <tr>
                    {{#each this}}
                    <td>{{this}}</td>
                    {{/each}}
                </tr>
                {{/each}}
            </tbody>
        </table>

        {{/if}}


    </div>



</body>

</html>